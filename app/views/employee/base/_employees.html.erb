<div class='span4'>
  <div class='well well-user well-transform-container'>
    <h2>Users</h2>
    <p>Here you can...</p>
    <ul>
      <%= content_tag :li, link_to('manage users', employee_users_path) if current_user.is_able_to(:view, Employee.new) %>
      <%= content_tag :li, link_to('create a new user', new_employee_user_path) if current_user.is_able_to(:create, Employee.new) %>
    </ul>
    <b class='well-transformer'></b>
  </div>
</div>