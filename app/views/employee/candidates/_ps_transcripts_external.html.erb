<div class='collapse-group'>
  <div class='collapse-heading'>
    <%= link_to 'Transcripts (External)', '#', class: 'collapse-toggle', data: { toggle: 'collapse', target: ".collapse-transcripts-external" } %>
  </div>
  <div class='collapse collapse-transcripts-external'>
    <div class='collapse-content'>
      <div class='tabbable tabs-left'>
        <ul class='nav nav-tabs'>
          <%- @peoplesoft_candidate.external_transcripts.sort_by {|trans| trans[:descr_1] }.each_with_index do |record, index| -%>
          <li class='<%= 'active' if index.zero? %>'><%= link_to record[:descr_1], "##{record[:crse_id]}", class: 'affixable', data: { toggle: 'tab'} %></li>
          <%- end -%>
        </ul>
        <div class='tab-content'>
          <%- @peoplesoft_candidate.external_transcripts.each_with_index do |record, index| -%>
          <div class='tab-pane <%= 'active' if index.zero? %>' id='<%= record[:crse_id] %>'>
            <table class='table table-striped table-bordered'>
              <tbody>
                <%- record.each_pair do |kee, val| -%>
                <tr>
                  <td class='attr'><%= kee.to_s.titleize %></td>
                  <td><%= val %></td>
                </tr>
                <%- end -%>
              </tbody>
            </table>
          </div>
          <%- end -%>
        </div>
      </div>
    </div>
  </div>
</div>